SHELL	:= /bin/bash
PATH	:= /opt/cross/usr/bin:$(PATH)

TOP		:= $(PWD)

ARCH	:= i386
TARGET	:= i686-aplus
PREFIX	:= $(TOP)/bin


CC		:= $(TARGET)-gcc
CXX		:= $(TARGET)-g++
ASM		:= nasm
AS		:= $(TARGET)-as

LD		:= $(TARGET)-ld
OBJCPY	:= $(TARGET)-objcopy

ZIP		:= gzip -f
CP		:= cp
MV		:= mv


DEFINES	:= -DKERNEL -D__aplus__ -D__$(ARCH)__ -DARCH=\"$(ARCH)\"
LIBS	:= -lc -lm -lgcc
WARN	:= -Wno-implicit-function-declaration -Wno-strict-aliasing -Wno-pragmas -Wall

CFLAGS	:= $(DEFINES) $(WARN) -pipe -fpack-struct -I $(TOP)/src/include -I $(TOP)/src/include/lwip -c -nostdlib -std=c99 -O2
CXXFLAGS:= $(DEFINES) $(WARN) -pipe -fpack-struct -I $(TOP)/src/include -I $(TOP)/src/include/lwip -c -nostdlib
AFLAGS	:= $(DEFINES) -f elf
ASFLAGS	:= --defsym __$(ARCH)__=0x0 -c
LFLAGS	:= -T $(TOP)/src/link/$(ARCH).ld -Map aplus.map -L $(LIBS)

include Makefile.$(ARCH)

CROSSLIB:= /opt/cross/usr
